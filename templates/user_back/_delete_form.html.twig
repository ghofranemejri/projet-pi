<form method="post" action="{{ path('admin_back_user_delete', {'id': user.id}) }}" onsubmit="return confirm('Confirmer la suppression ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
    <button type="submit">Supprimer</button>
</form>
