<form method="post" action="{{ path('app_form_back_delete', {'id': form_post.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ form_post.id) }}">
    <button class="btn">Delete</button>
</form>
