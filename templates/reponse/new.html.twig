{% extends 'base.html.twig' %}

{% block title %}Créer une nouvelle Réponse{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1 class="mb-4">Créer une nouvelle Réponse</h1>

        {{ form_start(form) }}
            {{ form_widget(form) }}
        {{ form_end(form) }}

        <a href="{{ path('app_reponse_index') }}" class="btn btn-secondary mt-3">Retour à la liste</a>
    </div>
{% endblock %}
