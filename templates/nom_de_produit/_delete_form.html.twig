<form method="post" action="{{ path('app_nom_de_produit_delete', {'id': nom_de_produit.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ nom_de_produit.id) }}">
    <button class="btn">Delete</button>
</form>
