<form method="post" action="{{ path('app_disponibilite_delete_back', {'id': disponibilite.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ disponibilite.id) }}">
    <button class="btn" style="background-color: #007bff; color: white; font-size: 1.5rem; padding: 8px 15px; border-radius: 50px; display: flex; align-items: center; justify-content: center; transition: background-color 0.3s ease;">
        🗑️
    </button>
</form>
