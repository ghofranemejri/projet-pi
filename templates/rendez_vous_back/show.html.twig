{% extends 'base2.html.twig' %}

{% block title %}RendezVous{% endblock %}

{% block content %}
    <h1>RendezVous</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rendez_vou.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ rendez_vou.date ? rendez_vou.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ rendez_vou.statut }}</td>
            </tr>
            <tr>
                <th>Motif</th>
                <td>{{ rendez_vou.motif }}</td>
            </tr>
            <tr>
                <th>Date_creation</th>
                <td>{{ rendez_vou.dateCreation ? rendez_vou.dateCreation|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_rendez_vous_index_back') }}">back to list</a>

    <a href="{{ path('app_rendez_vous_edit_back', {'id': rendez_vou.id}) }}">edit</a>

    {{ include('rendez_vous_back/_delete_form.html.twig') }}
{% endblock %}
